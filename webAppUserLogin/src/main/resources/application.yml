spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userinfo
    username: root
    password: Chicago123@

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

